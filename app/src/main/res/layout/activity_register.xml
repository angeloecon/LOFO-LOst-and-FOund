<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Register">

    <!-- 1. Background Image View (using the same image) -->
    <ImageView
        android:id="@+id/background_image"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        android:src="@drawable/your_background_image"
        android:contentDescription="@null"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 2. Gradient Overlay View (using the subtle maroon/blue gradient) -->
    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/bg_gradient_overlay"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 3. Register CardView (Centered on screen) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/registerCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="30dp"
        app:cardCornerRadius="30dp"
        app:cardElevation="15dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.5"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="32dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_gravity="center_horizontal"
            android:padding="28dp"
            android:background="@android:color/white">

            <!-- Title - Styled to match Login style (28sp, Maroon) -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Create Account"
                android:textSize="28sp"
                android:textAlignment="center"
                android:textStyle="bold"
                android:textColor="#881A3F"/>

            <!-- Subtitle - Added for consistency with Login -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Start your journey with us"
                android:textSize="14sp"
                android:textAlignment="center"
                android:textColor="#616161"
                android:layout_marginBottom="24dp"/>

            <!-- Name Input (Height adjusted to 56dp for consistency) -->
            <EditText
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:id="@+id/signup_name"
                android:background="@drawable/input_border_rounded"
                android:layout_marginTop="12dp"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:hint="Full Name"
                android:drawableStart="@drawable/baseline_person_24"
                android:drawablePadding="10dp"
                android:textColor="@android:color/black"
                android:textSize="14sp"/>

            <!-- Email Input (Height adjusted to 56dp for consistency) -->
            <EditText
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:id="@+id/signup_email"
                android:background="@drawable/input_border_rounded"
                android:layout_marginTop="16dp"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:hint="Email Address"
                android:drawableStart="@drawable/baseline_email_24"
                android:drawablePadding="10dp"
                android:textColor="@android:color/black"
                android:textSize="14sp"
                android:inputType="textEmailAddress"/>

            <!-- Phone Input (Height adjusted to 56dp for consistency) -->
            <EditText
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:id="@+id/signup_phone"
                android:background="@drawable/input_border_rounded"
                android:layout_marginTop="16dp"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:hint="Phone No"
                android:drawableStart="@drawable/baseline_phone_24"
                android:drawablePadding="10dp"
                android:textColor="@android:color/black"
                android:textSize="14sp"
                android:inputType="phone"/>

            <!-- Password Input (Height adjusted to 56dp for consistency) -->
            <EditText
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:id="@+id/signup_password"
                android:background="@drawable/input_border_rounded"
                android:layout_marginTop="16dp"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:hint="Password"
                android:inputType="textPassword"
                android:drawableStart="@drawable/baseline_lock_24"
                android:drawablePadding="10dp"
                android:textColor="@android:color/black"
                android:textSize="14sp"/>

            <!-- Sign Up Button (Updated to gradient and tint fixes) -->
            <Button
                android:id="@+id/signup_button"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="SIGN UP"
                android:textSize="18sp"
                android:layout_marginTop="32dp"
                android:background="@drawable/btn_gradient_login"
                android:textColor="@android:color/white"
                android:textStyle="bold"
                android:letterSpacing="0.05"
                android:stateListAnimator="@null"
                app:backgroundTint="@null"/>

            <!-- Login Redirect Link (Styled to match Login screen) -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_gravity="center"
                android:layout_marginTop="16dp">
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:id="@+id/loginRedirectText"
                    android:text="Already have an account? "
                    android:textColor="#616161"
                    android:textSize="14sp"/>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Login"
                    android:id="@+id/loginLink"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="#1A237E"
                    android:clickable="true"
                    android:focusable="true"/>
            </LinearLayout>

        </LinearLayout>
    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>